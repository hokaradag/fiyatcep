---
phase: cross-phase-uat-bugfixes
task: 0
total_tasks: 2
status: not_started
last_updated: 2026-03-30T11:52:05.692Z
---

<current_state>
All 4 phases complete. Phase 04 human UAT passed 5/5 tests (committed d3f1252). Two diagnosed bugs remain before milestone completion.
</current_state>

<completed_work>

- Phase 01 (quality foundation): COMPLETE — but UAT audit found 1 unresolved bug (Turkish char normalization)
- Phase 02 (data layer): COMPLETE
- Phase 03 (price comparison + market detail): COMPLETE — but UAT audit found 1 unresolved bug (market brand colors)
- Phase 04 (cart comparison): COMPLETE and verified (13/13 must-haves, committed 966574f)
- Cross-phase UAT audit + bug diagnosis: COMPLETE (committed 24f88e8)
- Phase 04 human UAT: COMPLETE — 5/5 passed (committed d3f1252)
</completed_work>

<remaining_work>

- Bug Fix 1 (Phase 01 UAT issue): Turkish character normalization — `İ` (U+0130) not handled
  - Fix: Add `.replaceAll('İ', 'i')` before `.toLowerCase()` in `lib/core/utils/text_normalizer.dart`
  - Fix: Remove duplicate `_normalizeText()` in `lib/features/discounts/discounts_page.dart` (lines 34-44), replace with `TextNormalizer.normalize()`
  - Root cause already diagnosed in `01-UAT.md`

- Bug Fix 2 (Phase 03 UAT issue): Market brand colors show grey instead of brand color
  - Fix: Change mock market IDs in `lib/features/markets/data/mock_markets.dart` from `m1–m5` to slugs (`migros`, `a101`, `bim`, `sok`, `carrefoursa`)
  - Verify: Downstream providers filtering by `market.id` still work after ID change
  - Root cause already diagnosed in `03-UAT.md`
</remaining_work>

<decisions_made>

- Phase 04 architecture: CartNotifier clones FavoritesNotifier pattern using SharedPreferences key 'cart_products'
- Cart uses id-based dedup (`p.id == product.id`) to avoid Freezed equality pitfall with priceHistory
- cartComparisonProvider uses `ref.watch` for cart (sync) + `ref.read` inside `Future.wait` for prices (async)
- Market comparison sorted by partialTotal ascending; 0-match markets go to bottom
</decisions_made>

<blockers>
- Bug Fix 2 carries a small risk: downstream providers/widgets that filter by `market.id` (string match) need verification after slug ID change — check all usages before committing
</blockers>

<context>
All four phases implemented, verified, and human-tested. Two post-implementation UAT bugs diagnosed during `/gsd:audit-uat` — both have clear root causes. These are the only remaining items before `/gsd:complete-milestone`.
</context>

<next_action>
Start with: Fix Bug 1 (Turkish char normalization) — edit `lib/core/utils/text_normalizer.dart` to add `.replaceAll('İ', 'i')` before `.toLowerCase()`, then remove the duplicate `_normalizeText()` in `discounts_page.dart`.
Then fix Bug 2 (market brand colors) — change mock market IDs to slugs and verify downstream.
Use `/gsd:quick` for each fix.
</next_action>
