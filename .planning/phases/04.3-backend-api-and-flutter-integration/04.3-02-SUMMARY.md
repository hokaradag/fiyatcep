---
phase: 04.3-backend-api-and-flutter-integration
plan: "02"
subsystem: flutter-remote-wiring
tags: [flutter, riverpod, dio, remote-datasource, favorites, qual-06]
dependency_graph:
  requires: []
  provides: [flutter-remote-datasources, product-prices-endpoint-wiring, favorites-cleanup]
  affects: [products-provider, markets-provider, discounts-provider, favorites-provider]
tech_stack:
  added: []
  patterns: [remote-datasource-swap, abstract-interface-extension, async-notifier-only-favorites]
key_files:
  created: []
  modified:
    - lib/shared/providers/api_client_provider.dart
    - lib/features/products/data/datasources/product_datasource.dart
    - lib/features/products/data/datasources/product_remote_datasource.dart
    - lib/features/products/data/datasources/product_mock_datasource.dart
    - lib/features/products/presentation/providers/products_provider.dart
    - lib/shared/providers/repository_providers.dart
  deleted:
    - lib/features/favorites/data/favorites_store.dart
decisions:
  - "getProductPrices stub in ProductMockDataSourceImpl returns empty list — mock datasource no longer used in production wiring, stub satisfies interface contract"
  - "Base URL set to http://10.0.2.2:8000/api/v1 for Android emulator; LAN_IP comment added for physical device testing"
metrics:
  duration_minutes: 3
  completed_date: "2026-04-09"
  tasks_completed: 2
  files_modified: 7
requirements_addressed: [INTEG-02, INTEG-03, QUAL-06]
---

# Phase 04.3 Plan 02: Flutter Remote Wiring Summary

Flutter connected to live FastAPI backend — all 3 repository providers (products, markets, discounts) now use remote datasources; getProductPrices() wired from abstract interface through ProductRemoteDataSourceImpl to productMarketPricesProvider; FavoritesStore singleton deleted completing QUAL-06.

## Tasks Completed

| # | Name | Commit | Files |
|---|------|--------|-------|
| 1 | Add getProductPrices(), update base URL, flip mock to remote, rewire productMarketPricesProvider | 49ff70d | api_client_provider.dart, product_datasource.dart, product_remote_datasource.dart, products_provider.dart, repository_providers.dart |
| 2 | Delete deprecated FavoritesStore and verify no remaining imports (QUAL-06) | 079d5b6 | favorites_store.dart (deleted), product_mock_datasource.dart |

## What Was Built

- **Base URL updated** from `https://api.fiyatcep.com/api/v1` to `http://10.0.2.2:8000/api/v1` with comments for emulator vs physical device
- **`getProductPrices(String productId)`** added to `ProductRemoteDataSource` abstract interface and implemented in `ProductRemoteDataSourceImpl` calling `GET /products/{id}/prices`
- **`productMarketPricesProvider`** rewired from mock data map to `ref.watch(productRemoteDataSourceProvider).getProductPrices(productId)` with price-ascending sort preserved
- **All 3 repository providers** (products, markets, discounts) changed from `MockDataSourceImpl()` to `ref.watch(*RemoteDataSourceProvider)` — mock imports removed
- **FavoritesStore** (deprecated singleton with ValueNotifier) deleted; `FavoritesNotifier` (AsyncNotifier) confirmed as sole favorites state manager

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] ProductMockDataSourceImpl missing getProductPrices() implementation**
- **Found during:** Task 2 dart analyze after FavoritesStore deletion
- **Issue:** Adding `getProductPrices()` to `ProductRemoteDataSource` abstract interface in Task 1 broke `ProductMockDataSourceImpl` which implements the same interface — `dart analyze lib/` reported `non_abstract_class_inherits_abstract_member`
- **Fix:** Added stub `getProductPrices()` returning empty list to `ProductMockDataSourceImpl` with 400ms delay to satisfy interface contract; mock datasource is no longer used in production wiring
- **Files modified:** `lib/features/products/data/datasources/product_mock_datasource.dart`
- **Commit:** 079d5b6

## Known Stubs

| File | Stub | Reason |
|------|------|--------|
| `lib/features/products/data/datasources/product_mock_datasource.dart` | `getProductPrices()` returns `[]` | Mock datasource no longer wired in production; stub satisfies interface contract only. Remote datasource is the live path. |

## Verification

- `dart analyze lib/` — no issues found
- Base URL: `http://10.0.2.2:8000/api/v1` confirmed in api_client_provider.dart
- No MockDataSourceImpl references remain in repository_providers.dart
- FavoritesStore file deleted; grep confirms zero imports of `favorites_store` in lib/

## Self-Check: PASSED

- [x] `lib/shared/providers/api_client_provider.dart` — exists, contains `10.0.2.2:8000`
- [x] `lib/features/products/data/datasources/product_datasource.dart` — exists, contains `getProductPrices`
- [x] `lib/features/products/data/datasources/product_remote_datasource.dart` — exists, contains `getProductPrices` with `/products/$productId/prices`
- [x] `lib/features/products/presentation/providers/products_provider.dart` — exists, contains `productRemoteDataSourceProvider`, no `mockMarketPrices`
- [x] `lib/shared/providers/repository_providers.dart` — exists, contains `ref.watch(productRemoteDataSourceProvider)`, no `MockDataSourceImpl`
- [x] `lib/features/favorites/data/favorites_store.dart` — DELETED (confirmed)
- [x] Commits 49ff70d and 079d5b6 — confirmed in git log
