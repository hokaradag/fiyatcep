---
phase: 04.3-backend-api-and-flutter-integration
plan: 03
type: execute
wave: 2
depends_on: ["04.3-01", "04.3-02"]
files_modified: []
autonomous: false
requirements:
  - INTEG-01
  - INTEG-02
  - INTEG-03

must_haves:
  truths:
    - "Flutter Products tab shows real Migros products from backend"
    - "Market Detail page for Migros shows products fetched from API"
    - "Price History chart populates with real price data"
    - "Cart Comparison works with real product IDs"
    - "Non-Migros markets show empty state (no fake data)"
  artifacts: []
  key_links:
    - from: "Flutter app"
      to: "FastAPI backend"
      via: "HTTP /api/v1/*"
      pattern: "10.0.2.2:8000"
---

<objective>
End-to-end verification: run backend with scraped data, launch Flutter on Android emulator, confirm real Migros data flows through all screens.

Purpose: Validates the entire integration chain — backend API, Flutter remote datasources, data rendering.
Output: Human-verified working e2e flow.
</objective>

<execution_context>
@C:/dev/fiyatcep/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/fiyatcep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04.3-backend-api-and-flutter-integration/04.3-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Start backend, trigger Migros scrape, verify API returns data</name>
  <files></files>
  <read_first>
    backend/app/main.py,
    backend/app/routers/admin.py
  </read_first>
  <action>
Run the backend and verify API endpoints return real data:

1. Start backend: `cd C:/dev/fiyatcep/backend && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000` (run in background).
2. Wait for startup, then trigger Migros scrape: `curl -X POST http://localhost:8000/admin/scrape/run`. This returns `{"status": "scrape_started"}` immediately; scrape runs in background.
3. Wait ~30 seconds for scrape to complete (Migros scraper fetches 3 keywords x 2 pages).
4. Verify products endpoint: `curl http://localhost:8000/api/v1/products | python -m json.tool | head -30`. Should return JSON with `data` array containing products with `marketId: "migros"` and non-empty `priceHistory`.
5. Verify markets endpoint: `curl http://localhost:8000/api/v1/markets | python -m json.tool | head -30`. Should return 7 markets.
6. Verify discounts endpoint: `curl http://localhost:8000/api/v1/discounts | python -m json.tool | head -20`. May return empty if no discounted products, but should return `{"data": [...]}`.
7. Test cross-market prices: pick a product ID from step 4, call `curl http://localhost:8000/api/v1/products/{id}/prices | python -m json.tool`. Should return at least 1 market price.
8. Stop the background uvicorn process.

If any endpoint returns unexpected shape or error, diagnose and fix before proceeding to checkpoint.
  </action>
  <verify>
    <automated>cd C:/dev/fiyatcep/backend && python -c "
from fastapi.testclient import TestClient
from app.main import app
client = TestClient(app)
r = client.get('/api/v1/markets')
assert r.status_code == 200
data = r.json()['data']
assert len(data) == 7
print(f'Markets endpoint OK: {len(data)} markets')
for m in data[:2]:
    print(f'  {m[\"id\"]}: {m[\"name\"]}')
"</automated>
  </verify>
  <acceptance_criteria>
    - `curl http://localhost:8000/api/v1/markets` returns 200 with 7 market objects
    - `curl http://localhost:8000/api/v1/products` returns 200 with `data` array (may be empty before scrape)
    - All responses have `data` key in JSON envelope
  </acceptance_criteria>
  <done>Backend API serves correctly shaped responses for all endpoints. Markets seed data visible. Products available after scrape trigger.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Manual Flutter verification on Android emulator</name>
  <files></files>
  <action>
Human verifies the full backend-to-Flutter integration. FastAPI serves 11 API routes with real Migros data. Flutter connects via remote datasources at http://10.0.2.2:8000/api/v1.

Prerequisites:
1. Start backend: `cd C:/dev/fiyatcep/backend && python -m uvicorn app.main:app --host 0.0.0.0 --port 8000`
2. Trigger scrape (if not already done): `curl -X POST http://localhost:8000/admin/scrape/run` — wait ~30s
3. Launch Flutter on Android emulator: `cd C:/dev/fiyatcep && flutter run`

Verification steps:
1. Products tab: Opens and shows product cards with real Migros data (product names in Turkish, real prices in TL). NOT mock data.
2. Product detail: Tap any product — detail page loads with price and market info.
3. Price History chart: On product detail, price history section shows chart with data points (not empty).
4. Cross-market prices: On product detail, market prices section shows at least Migros price.
5. Markets tab: Shows 7 markets. Tap Migros — market detail loads with Migros products.
6. Non-Migros market: Tap A101 or BIM — should show empty product list (EmptyState widget), not crash.
7. Discounts tab: Shows discounted Migros products (if any). May be empty.
8. Cart: Add a product to cart, go to Cart Comparison — should work with real product IDs.
9. Favorites: Add/remove a product — FavoritesNotifier handles it (FavoritesStore deleted).
  </action>
  <verify>
    <automated>echo "CHECKPOINT: Requires human verification on Android emulator"</automated>
  </verify>
  <acceptance_criteria>
    - Products tab shows real Migros product data, not mock data
    - Market Detail for Migros shows real products
    - Non-Migros markets show empty state without crashing
    - Favorites add/remove works without FavoritesStore
  </acceptance_criteria>
  <done>Human confirmed all 9 verification steps pass on Android emulator. E2E integration verified.</done>
</task>

</tasks>

<verification>
- Backend pytest tests pass (from Plan 01)
- Backend API serves real data after scrape
- Flutter displays real Migros data on emulator
- All tabs functional (Products, Markets, Discounts, Cart, Favorites)
</verification>

<success_criteria>
- End-to-end data flow verified: scraper -> DB -> API -> Flutter
- Real Migros product data visible in Flutter UI
- Non-Migros markets gracefully show empty state
- Cart and Favorites work with real product IDs
</success_criteria>

<output>
After completion, create `.planning/phases/04.3-backend-api-and-flutter-integration/04.3-03-SUMMARY.md`
</output>
