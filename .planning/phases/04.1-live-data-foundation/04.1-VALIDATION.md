---
phase: 04.1
slug: live-data-foundation
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-04-01
---

# Phase 04.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | none — this phase produces spec documents (API-CONTRACT.md, DB-SCHEMA.sql), not executable code |
| **Config file** | none |
| **Quick run command** | `grep -c "##" .planning/phases/04.1-live-data-foundation/04.1-API-CONTRACT.md` |
| **Full suite command** | manual review of both spec files against acceptance criteria |
| **Estimated runtime** | ~5 seconds (grep checks) |

---

## Sampling Rate

- **After every task commit:** Run quick grep checks to verify file structure
- **After every plan wave:** Manual review against CONTEXT.md decisions
- **Before `/gsd:verify-work`:** Both spec files present and complete
- **Max feedback latency:** 10 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Requirement | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------|-----------|-------------------|-------------|--------|
| 04.1-01-01 | 01 | 1 | D-08 | file-exists | `test -f .planning/phases/04.1-live-data-foundation/API-CONTRACT.md && echo OK` | ❌ W0 | ⬜ pending |
| 04.1-01-02 | 01 | 1 | D-10 | grep | `grep -q '"data":' .planning/phases/04.1-live-data-foundation/API-CONTRACT.md && echo OK` | ❌ W0 | ⬜ pending |
| 04.1-01-03 | 01 | 1 | D-05 | grep | `grep -q '/products/{id}/prices' .planning/phases/04.1-live-data-foundation/API-CONTRACT.md && echo OK` | ❌ W0 | ⬜ pending |
| 04.1-02-01 | 02 | 1 | D-08 | file-exists | `test -f .planning/phases/04.1-live-data-foundation/DB-SCHEMA.sql && echo OK` | ❌ W0 | ⬜ pending |
| 04.1-02-02 | 02 | 1 | D-01 | grep | `grep -q 'CREATE TABLE market_products' .planning/phases/04.1-live-data-foundation/DB-SCHEMA.sql && echo OK` | ❌ W0 | ⬜ pending |
| 04.1-02-03 | 02 | 1 | D-03 | grep | `grep -q 'CREATE TABLE price_history' .planning/phases/04.1-live-data-foundation/DB-SCHEMA.sql && echo OK` | ❌ W0 | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] Both spec files (`API-CONTRACT.md`, `DB-SCHEMA.sql`) created in `.planning/phases/04.1-live-data-foundation/`

*No test framework installation needed — deliverables are documentation files verified by grep/file-exists checks.*

---

## Manual-Only Verifications

| Behavior | Requirement | Why Manual | Test Instructions |
|----------|-------------|------------|-------------------|
| camelCase JSON field names match Flutter models | D-02, D-10 | Requires visual cross-reference against Dart source | Compare API-CONTRACT.md field names against `ProductItem`, `MarketPriceItem`, `DiscountItem` Freezed models |
| Market slugs match Flutter marketBrands | D-04 | String comparison across files | Verify `markets` table `id` values in DB-SCHEMA.sql match slugs in `lib/features/markets/data/market_brand_config.dart` |
| `/products/{id}/prices` uses market_products.id | D-05 | Semantic correctness | Verify endpoint description explicitly states param is `market_products.id`, not `products.id` |
| DateTime fields use UTC ISO 8601 with Z suffix | D-03, D-11 | Example format verification | Check all datetime example values in API-CONTRACT.md end with `Z` |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 10s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
