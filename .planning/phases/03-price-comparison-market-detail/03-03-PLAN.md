---
phase: 03-price-comparison-market-detail
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - lib/features/markets/widgets/market_detail_header_widget.dart
autonomous: true
requirements: [MKTD-01]

must_haves:
  truths:
    - "Market detail page shows brand color banner at top for known markets"
    - "Logo placeholder (Icons.store) renders inside white container overlapping banner bottom edge"
    - "Unknown market ID falls back to grey banner and grey store icon"
    - "Existing Market Info Card and Statistics Card remain unchanged below the banner"
  artifacts:
    - path: "lib/features/markets/widgets/market_detail_header_widget.dart"
      provides: "Brand banner + logo rendering in market detail header"
      contains: "marketBrands"
  key_links:
    - from: "lib/features/markets/widgets/market_detail_header_widget.dart"
      to: "lib/features/markets/data/market_brand_config.dart"
      via: "import and map lookup"
      pattern: "marketBrands\\[market\\.id\\]"
---

<objective>
Add brand banner and logo to MarketDetailHeaderWidget using the MarketBrand config map.

Purpose: Enriches the market detail page with visual branding (colored banner, logo placeholder) per MKTD-01, making each market's page visually distinct.
Output: Updated MarketDetailHeaderWidget with banner block at top.
</objective>

<execution_context>
@C:/dev/fiyatcep/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/fiyatcep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-price-comparison-market-detail/03-CONTEXT.md
@.planning/phases/03-price-comparison-market-detail/03-UI-SPEC.md
@.planning/phases/03-price-comparison-market-detail/03-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/features/markets/data/market_brand_config.dart (created in Plan 01):
```dart
class MarketBrand {
  final Color primaryColor;
  final String? logoAsset;    // null = use fallback icon
  final String? bannerAsset;  // null = use solid primaryColor block
  const MarketBrand({required this.primaryColor, this.logoAsset, this.bannerAsset});
}

const Map<String, MarketBrand> marketBrands = {
  'migros': MarketBrand(primaryColor: Color(0xFF004A97)),
  'a101': MarketBrand(primaryColor: Color(0xFFE30613)),
  'bim': MarketBrand(primaryColor: Color(0xFF003DA5)),
  'carrefoursa': MarketBrand(primaryColor: Color(0xFF004B99)),
  'sok': MarketBrand(primaryColor: Color(0xFFE2001A)),
  'tarim-kredi': MarketBrand(primaryColor: Color(0xFF007A33)),
  'file-market': MarketBrand(primaryColor: Color(0xFFF58220)),
};
```

From lib/features/markets/models/market_item.dart:
```dart
@freezed
class MarketItem with _$MarketItem {
  const factory MarketItem({
    required String id,       // <-- key for marketBrands lookup
    required String name,
    ...
  }) = _MarketItem;
}
```

From lib/features/markets/widgets/market_detail_header_widget.dart (current):
```dart
class MarketDetailHeaderWidget extends StatelessWidget {
  final MarketItem market;
  final int productCount;
  final int discountCount;

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        // Market Info Card (Card with elevation 2, name, description, chips)
        Card(...),
        const SizedBox(height: 16),
        // Statistics Card (Card with elevation 1, product count, discount count)
        Card(...),
      ],
    );
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add brand banner and logo block to MarketDetailHeaderWidget</name>
  <files>lib/features/markets/widgets/market_detail_header_widget.dart</files>
  <read_first>
    - C:/dev/fiyatcep/lib/features/markets/widgets/market_detail_header_widget.dart
    - C:/dev/fiyatcep/lib/features/markets/data/market_brand_config.dart
    - C:/dev/fiyatcep/lib/features/markets/models/market_item.dart
    - C:/dev/fiyatcep/.planning/phases/03-price-comparison-market-detail/03-UI-SPEC.md
  </read_first>
  <action>
Edit `lib/features/markets/widgets/market_detail_header_widget.dart` per D-05, D-13, and UI-SPEC Component Inventory:

1. Add import at top:
```dart
import '../data/market_brand_config.dart';
```

2. In the `build()` method, insert a brand banner block at the beginning of the `Column.children` list, BEFORE the existing Market Info Card. The current Column starts at line 44.

Insert these children at the start of the Column's children list:

```dart
// Brand banner + logo block
Builder(builder: (context) {
  final brand = marketBrands[market.id];
  return Stack(
    clipBehavior: Clip.none,
    children: [
      // Banner: solid brand color (or banner image if available)
      Container(
        height: 120,
        width: double.infinity,
        decoration: BoxDecoration(
          color: brand?.primaryColor ?? Colors.grey.shade300,
          borderRadius: const BorderRadius.only(
            topLeft: Radius.circular(18),
            topRight: Radius.circular(18),
          ),
        ),
        child: brand?.bannerAsset != null
            ? ClipRRect(
                borderRadius: const BorderRadius.only(
                  topLeft: Radius.circular(18),
                  topRight: Radius.circular(18),
                ),
                child: Image.asset(brand!.bannerAsset!, fit: BoxFit.cover,
                    width: double.infinity, height: 120),
              )
            : null,
      ),
      // Logo: white container overlapping bottom edge of banner
      Positioned(
        bottom: -28,
        left: 16,
        child: Container(
          width: 56,
          height: 56,
          decoration: BoxDecoration(
            color: Colors.white,
            borderRadius: BorderRadius.circular(12),
            boxShadow: const [
              BoxShadow(blurRadius: 4, color: Colors.black26),
            ],
          ),
          child: brand?.logoAsset != null
              ? ClipRRect(
                  borderRadius: BorderRadius.circular(12),
                  child: Image.asset(brand!.logoAsset!, fit: BoxFit.contain),
                )
              : Icon(
                  Icons.store,
                  color: brand?.primaryColor ?? Colors.grey.shade600,
                  size: 28,
                ),
        ),
      ),
    ],
  );
}),
const SizedBox(height: 36), // 28px overlap + 8px gap
```

3. The existing `Card(...)` (Market Info Card starting at line 47) and `SizedBox(height: 16)` + Statistics Card remain unchanged after the new banner block.

4. The full Column.children order after edit:
   - Brand banner block (NEW)
   - SizedBox(height: 36) (NEW)
   - Market Info Card (EXISTING — unchanged)
   - SizedBox(height: 16) (EXISTING — unchanged)
   - Statistics Card (EXISTING — unchanged)

5. Run `flutter analyze` to verify.
  </action>
  <verify>
    <automated>cd C:/dev/fiyatcep && flutter analyze lib/features/markets/widgets/market_detail_header_widget.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - market_detail_header_widget.dart contains `import '../data/market_brand_config.dart';`
    - market_detail_header_widget.dart contains `marketBrands[market.id]`
    - market_detail_header_widget.dart contains `Container(` with `height: 120`
    - market_detail_header_widget.dart contains `Positioned(` with `bottom: -28`
    - market_detail_header_widget.dart contains `width: 56` and `height: 56` (logo container)
    - market_detail_header_widget.dart contains `Icons.store` (fallback icon)
    - market_detail_header_widget.dart contains `Colors.grey.shade300` (fallback banner color)
    - market_detail_header_widget.dart contains `SizedBox(height: 36)`
    - market_detail_header_widget.dart still contains `Listelenen Ürün` (statistics card preserved)
    - market_detail_header_widget.dart still contains `Aktif İndirim` (statistics card preserved)
    - flutter analyze reports no errors
  </acceptance_criteria>
  <done>MarketDetailHeaderWidget displays brand-colored banner (120px) with overlapping logo container (56x56) for all 7 markets. Unknown markets fall back to grey banner + Icons.store. Existing info/stats cards unchanged below.</done>
</task>

</tasks>

<verification>
- `flutter analyze` passes on market_detail_header_widget.dart
- `flutter test --no-pub` — existing tests still pass
- MarketDetailPage renders brand banner with correct color for known markets (MKTD-01)
</verification>

<success_criteria>
- Market detail page shows colored banner at top matching market brand color (MKTD-01)
- Logo area shows Icons.store fallback in white container (MKTD-01)
- Unknown market ID shows grey banner and grey icon (MKTD-01)
- Existing Market Info Card and Statistics Card still render correctly below banner
</success_criteria>

<output>
After completion, create `.planning/phases/03-price-comparison-market-detail/03-03-SUMMARY.md`
</output>
