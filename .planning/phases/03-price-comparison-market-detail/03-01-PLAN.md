---
phase: 03-price-comparison-market-detail
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - pubspec.yaml
  - lib/features/markets/data/market_brand_config.dart
  - lib/features/products/models/price_point.dart
  - lib/features/products/models/price_point.freezed.dart
  - lib/features/products/models/price_point.g.dart
  - assets/logos/.gitkeep
  - assets/banners/.gitkeep
  - test/features/products/presentation/price_comparison_widget_test.dart
  - test/features/products/presentation/price_history_chart_test.dart
  - test/features/markets/presentation/market_detail_page_test.dart
autonomous: true
requirements: [COMP-02, MKTD-01]

must_haves:
  truths:
    - "fl_chart package is installed and importable"
    - "MarketBrand config map contains all 7 Turkish markets with correct brand colors"
    - "PricePoint has a displayDate getter returning Turkish short date format"
    - "Asset directories are registered in pubspec.yaml"
    - "Price history chart widget can be built and imported without errors because fl_chart is installed and importable"
    - "Wave 0 stub test files exist and pass so downstream tasks have test scaffolds to extend"
  artifacts:
    - path: "lib/features/markets/data/market_brand_config.dart"
      provides: "MarketBrand class and marketBrands const map"
      exports: ["MarketBrand", "marketBrands"]
    - path: "lib/features/products/models/price_point.dart"
      provides: "PricePoint with displayDate getter"
      contains: "String get displayDate"
    - path: "assets/logos/.gitkeep"
      provides: "Logo asset directory placeholder"
    - path: "assets/banners/.gitkeep"
      provides: "Banner asset directory placeholder"
    - path: "test/features/products/presentation/price_comparison_widget_test.dart"
      provides: "Stub widget test for COMP-01 price comparison"
    - path: "test/features/products/presentation/price_history_chart_test.dart"
      provides: "Stub widget test for COMP-02 price history chart"
    - path: "test/features/markets/presentation/market_detail_page_test.dart"
      provides: "Stub widget test for MKTD-01 market detail branding"
  key_links:
    - from: "pubspec.yaml"
      to: "fl_chart"
      via: "dependency declaration"
      pattern: "fl_chart:"
    - from: "pubspec.yaml"
      to: "assets/logos/"
      via: "asset directory registration"
      pattern: "assets/logos/"
---

<objective>
Add fl_chart dependency, create MarketBrand config, add PricePoint.displayDate getter, register asset directories, and create Wave 0 stub test files for all Phase 3 requirements.

Purpose: This is the foundation plan that all Phase 3 UI work depends on. fl_chart must be installed before chart widgets can be written. MarketBrand config must exist before header widget can reference it. PricePoint needs a displayDate getter for chart tooltip date formatting. Stub test files must exist before Wave 1/2 tasks can run flutter test in their verify blocks.
Output: pubspec.yaml with fl_chart + assets registered, market_brand_config.dart, updated PricePoint model with displayDate getter, three stub test files.
</objective>

<execution_context>
@C:/dev/fiyatcep/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/fiyatcep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-price-comparison-market-detail/03-CONTEXT.md
@.planning/phases/03-price-comparison-market-detail/03-RESEARCH.md
@.planning/phases/03-price-comparison-market-detail/03-UI-SPEC.md

<interfaces>
<!-- Key types the executor needs -->

From lib/features/products/models/price_point.dart:
```dart
@freezed
class PricePoint with _$PricePoint {
  const factory PricePoint({
    required double price,
    required DateTime date,
  }) = _PricePoint;
  factory PricePoint.fromJson(Map<String, dynamic> json) => _$PricePointFromJson(json);
}
```
Note: PricePoint does NOT currently have a `const PricePoint._();` private constructor or a `displayDate` getter. Both must be added, then `dart run build_runner build` must be re-run.

From lib/features/markets/models/market_item.dart:
```dart
@freezed
class MarketItem with _$MarketItem {
  const factory MarketItem({
    required String id,       // <-- key used for marketBrands map lookup
    required String name,
    required String description,
    required int branchCount,
    required int activeDiscountCount,
    required bool supportsOnlineOrder,
    required bool hasLoyaltyProgram,
  }) = _MarketItem;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add fl_chart dependency, register asset directories, create asset placeholders</name>
  <files>pubspec.yaml, assets/logos/.gitkeep, assets/banners/.gitkeep</files>
  <read_first>
    - C:/dev/fiyatcep/pubspec.yaml
  </read_first>
  <action>
1. Edit `pubspec.yaml` — add `fl_chart: ^0.69.0` under the `dependencies:` section after `dio: ^5.3.0`. Using ^0.69.0 per D-01 baseline (safer than 1.x which requires Flutter SDK 3.27.4+ — the project SDK constraint is `^3.11.1` which does not guarantee 3.27.4+).

2. In the `flutter:` section of `pubspec.yaml`, uncomment/add the `assets:` block:
```yaml
flutter:
  uses-material-design: true
  assets:
    - assets/logos/
    - assets/banners/
```
Note: trailing slash is required for directory-level registration.

3. Create directory `assets/logos/` with an empty `.gitkeep` file.
4. Create directory `assets/banners/` with an empty `.gitkeep` file.
5. Run `flutter pub get` to install fl_chart.
  </action>
  <verify>
    <automated>cd C:/dev/fiyatcep && flutter pub get 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - pubspec.yaml contains `fl_chart: ^0.69.0` under dependencies
    - pubspec.yaml contains `assets/logos/` under flutter > assets
    - pubspec.yaml contains `assets/banners/` under flutter > assets
    - File assets/logos/.gitkeep exists
    - File assets/banners/.gitkeep exists
    - `flutter pub get` exits 0
  </acceptance_criteria>
  <done>fl_chart is installed, asset directories registered and created with .gitkeep placeholders</done>
</task>

<task type="auto">
  <name>Task 2: Create MarketBrand config and add PricePoint.displayDate getter</name>
  <files>lib/features/markets/data/market_brand_config.dart, lib/features/products/models/price_point.dart</files>
  <read_first>
    - C:/dev/fiyatcep/lib/features/products/models/price_point.dart
    - C:/dev/fiyatcep/lib/features/products/models/product_item.dart
  </read_first>
  <action>
1. Create `lib/features/markets/data/market_brand_config.dart` with the following content per D-03 and D-04:

```dart
import 'package:flutter/material.dart';

class MarketBrand {
  final Color primaryColor;
  final String? logoAsset;
  final String? bannerAsset;
  const MarketBrand({
    required this.primaryColor,
    this.logoAsset,
    this.bannerAsset,
  });
}

const Map<String, MarketBrand> marketBrands = {
  'migros': MarketBrand(primaryColor: Color(0xFF004A97)),
  'a101': MarketBrand(primaryColor: Color(0xFFE30613)),
  'bim': MarketBrand(primaryColor: Color(0xFF003DA5)),
  'carrefoursa': MarketBrand(primaryColor: Color(0xFF004B99)),
  'sok': MarketBrand(primaryColor: Color(0xFFE2001A)),
  'tarim-kredi': MarketBrand(primaryColor: Color(0xFF007A33)),
  'file-market': MarketBrand(primaryColor: Color(0xFFF58220)),
};
```
All `logoAsset` and `bannerAsset` are null initially — fallback rendering path (solid color + Icons.store) activates.

2. Edit `lib/features/products/models/price_point.dart` to add:
   - A private constructor `const PricePoint._();` (required by Freezed for custom getters on the class)
   - A `displayDate` getter using Turkish month names:

```dart
import 'package:freezed_annotation/freezed_annotation.dart';

part 'price_point.freezed.dart';
part 'price_point.g.dart';

@freezed
class PricePoint with _$PricePoint {
  const PricePoint._();

  const factory PricePoint({
    required double price,
    required DateTime date,
  }) = _PricePoint;

  factory PricePoint.fromJson(Map<String, dynamic> json) =>
      _$PricePointFromJson(json);

  static const _turkishMonths = [
    'Oca', 'Sub', 'Mar', 'Nis', 'May', 'Haz',
    'Tem', 'Agu', 'Eyl', 'Eki', 'Kas', 'Ara',
  ];

  String get displayDate => '${date.day} ${_turkishMonths[date.month - 1]}';
}
```

3. Run `dart run build_runner build --delete-conflicting-outputs` to regenerate Freezed files for PricePoint.

4. Run `flutter analyze lib/features/products/models/price_point.dart lib/features/markets/data/market_brand_config.dart` to verify no analysis errors.
  </action>
  <verify>
    <automated>cd C:/dev/fiyatcep && dart run build_runner build --delete-conflicting-outputs 2>&1 | tail -5 && flutter analyze lib/features/products/models/price_point.dart lib/features/markets/data/market_brand_config.dart 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/markets/data/market_brand_config.dart contains `class MarketBrand`
    - lib/features/markets/data/market_brand_config.dart contains `const Map<String, MarketBrand> marketBrands`
    - lib/features/markets/data/market_brand_config.dart contains `'migros': MarketBrand(primaryColor: Color(0xFF004A97))`
    - lib/features/markets/data/market_brand_config.dart contains 7 entries (migros, a101, bim, carrefoursa, sok, tarim-kredi, file-market)
    - lib/features/products/models/price_point.dart contains `const PricePoint._();`
    - lib/features/products/models/price_point.dart contains `String get displayDate`
    - lib/features/products/models/price_point.dart contains `_turkishMonths`
    - build_runner build exits 0
    - flutter analyze reports no errors for these files
  </acceptance_criteria>
  <done>MarketBrand config exists with 7 markets and correct colors. PricePoint has displayDate getter returning "dd MMM" in Turkish.</done>
</task>

<task type="auto">
  <name>Task 3: Create Wave 0 stub test files and verify mock data price variation</name>
  <files>test/features/products/presentation/price_comparison_widget_test.dart, test/features/products/presentation/price_history_chart_test.dart, test/features/markets/presentation/market_detail_page_test.dart</files>
  <read_first>
    - C:/dev/fiyatcep/lib/features/products/data/mock_market_prices.dart
  </read_first>
  <action>
1. Create directory `test/features/markets/presentation/` if it does not exist.

2. Create `test/features/products/presentation/price_comparison_widget_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('ProductPriceSection - price comparison', () {
    testWidgets('stub: price comparison widget scaffold exists', (tester) async {
      // Wave 0 stub — will be extended in Plan 02 with real widget tests for COMP-01
      expect(true, isTrue);
    });
  });
}
```

3. Create `test/features/products/presentation/price_history_chart_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('ProductPriceHistorySection - price history chart', () {
    testWidgets('stub: price history chart scaffold exists', (tester) async {
      // Wave 0 stub — will be extended in Plan 02 with real widget tests for COMP-02
      expect(true, isTrue);
    });
  });
}
```

4. Create `test/features/markets/presentation/market_detail_page_test.dart`:

```dart
import 'package:flutter_test/flutter_test.dart';

void main() {
  group('MarketDetailHeaderWidget - brand banner', () {
    testWidgets('stub: market detail page scaffold exists', (tester) async {
      // Wave 0 stub — will be extended in Plan 03 with real widget tests for MKTD-01
      expect(true, isTrue);
    });
  });
}
```

5. Verify that `lib/features/products/data/mock_market_prices.dart` contains at least 2 distinct prices for product ID 'p1'. The file should already have prices 79.90 (Migros), 74.95 (A101), and 78.50 (SOK) for 'p1'. If all prices for any single product ID are identical, add price variation. (Current mock data already has variation — this is a verification step, not an edit.)

6. Run all three stub test files to confirm they pass.
  </action>
  <verify>
    <automated>cd C:/dev/fiyatcep && flutter test test/features/products/presentation/price_comparison_widget_test.dart test/features/products/presentation/price_history_chart_test.dart test/features/markets/presentation/market_detail_page_test.dart --no-pub 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - test/features/products/presentation/price_comparison_widget_test.dart exists and contains `testWidgets`
    - test/features/products/presentation/price_history_chart_test.dart exists and contains `testWidgets`
    - test/features/markets/presentation/market_detail_page_test.dart exists and contains `testWidgets`
    - All three stub tests pass with `flutter test --no-pub`
    - mock_market_prices.dart contains at least 2 different price values for product 'p1' (e.g. 79.90 and 74.95)
  </acceptance_criteria>
  <done>Three Wave 0 stub test files exist and pass. Mock market prices verified to have price variation for COMP-01 diff label visibility.</done>
</task>

</tasks>

<verification>
- `flutter pub get` succeeds (fl_chart installed)
- `dart run build_runner build --delete-conflicting-outputs` succeeds (PricePoint regenerated)
- `flutter analyze` passes on all modified/new files
- `flutter test test/features/products/presentation/ test/features/markets/presentation/ --no-pub` — all stub tests pass
- `flutter test --no-pub` — existing tests still pass (no breaking changes)
</verification>

<success_criteria>
- fl_chart ^0.69.0 importable in Dart files
- MarketBrand config map has 7 entries with correct brand colors
- PricePoint.displayDate returns Turkish short date (e.g., "15 Oca")
- Asset directories registered in pubspec.yaml
- Three Wave 0 stub test files exist and pass
- Mock market prices contain varied prices for COMP-01 diff label demo
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-price-comparison-market-detail/03-01-SUMMARY.md`
</output>