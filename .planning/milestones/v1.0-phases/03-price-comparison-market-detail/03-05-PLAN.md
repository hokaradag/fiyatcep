---
phase: 03-price-comparison-market-detail
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/features/markets/data/datasources/market_mock_datasource.dart
  - lib/features/markets/data/mock_markets.dart
  - lib/features/products/data/datasources/product_mock_datasource.dart
  - lib/features/products/data/mock_products.dart
  - lib/features/products/data/mock_market_prices.dart
  - lib/features/discounts/data/datasources/discount_mock_datasource.dart
  - lib/features/discounts/data/mock_discounts.dart
  - test/features/markets/data/repositories/market_repository_test.dart
  - test/features/discounts/data/repositories/discount_repository_test.dart
  - test/features/discounts/models/discount_item_test.dart
  - test/features/products/data/repositories/product_repository_test.dart
  - test/features/products/presentation/pages/product_detail_page_test.dart
  - test/features/favorites/presentation/providers/favorites_notifier_test.dart
autonomous: true
requirements: [MKTD-01]
gap_closure: true

must_haves:
  truths:
    - "Market detail page shows a solid-color brand banner — Migros in blue (#004A97), A101 in red, BIM in dark blue, ŞOK in red, CarrefourSA in blue"
    - "Market detail page shows a white rounded square logo placeholder with a colored Icons.store icon (not grey — brand color matches the market)"
  artifacts:
    - path: "lib/features/markets/data/datasources/market_mock_datasource.dart"
      provides: "MarketItem entries with slug IDs: migros, a101, bim, sok, carrefoursa"
      contains: "id: 'migros'"
    - path: "lib/features/markets/data/mock_markets.dart"
      provides: "Consistent mock data with slug IDs"
      contains: "id: 'migros'"
  key_links:
    - from: "lib/features/markets/data/datasources/market_mock_datasource.dart"
      to: "lib/features/markets/data/market_brand_config.dart"
      via: "market.id used as key in marketBrands map lookup"
      pattern: "marketBrands\\[market\\.id\\]"
---

<objective>
Fix the market brand color gap: change mock market IDs from numeric slugs (m1–m5) to brand-name slugs (migros, a101, bim, sok, carrefoursa) so the marketBrands map lookup in MarketDetailHeaderWidget succeeds and renders the correct brand color.

Purpose: The MarketDetailHeaderWidget does `marketBrands[market.id]` to look up brand colors. The marketBrands map uses string keys like 'migros', 'a101', 'bim', 'sok', 'carrefoursa'. Mock market IDs use 'm1'–'m5', causing every lookup to return null and fall back to Colors.grey.shade300 (grey banner) and Colors.grey.shade600 (grey icon). Fixing the IDs resolves both UAT failures (test 5: banner color, test 6: logo icon color) in a single surgical change.

Output: All mock data files and tests updated with slug IDs. marketBrands lookup succeeds at runtime. Brand-colored banners and logo icons visible on market detail page.
</objective>

<execution_context>
@C:/dev/fiyatcep/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/fiyatcep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-price-comparison-market-detail/03-CONTEXT.md

<interfaces>
<!-- Key contracts the executor needs. Extracted from codebase. -->

From lib/features/markets/data/market_brand_config.dart:
```dart
const Map<String, MarketBrand> marketBrands = {
  'migros': MarketBrand(primaryColor: Color(0xFF004A97)),
  'a101': MarketBrand(primaryColor: Color(0xFFE30613)),
  'bim': MarketBrand(primaryColor: Color(0xFF003DA5)),
  'carrefoursa': MarketBrand(primaryColor: Color(0xFF004B99)),
  'sok': MarketBrand(primaryColor: Color(0xFFE2001A)),
  'tarim-kredi': MarketBrand(primaryColor: Color(0xFF007A33)),
  'file-market': MarketBrand(primaryColor: Color(0xFFF58220)),
};
```

From lib/features/markets/widgets/market_detail_header_widget.dart (line 49):
```dart
final brand = marketBrands[market.id];  // null when market.id is 'm1'–'m5'
```

ID mapping to apply everywhere:
  m1 → migros
  m2 → a101
  m3 → bim
  m4 → sok
  m5 → carrefoursa
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update market mock data files with slug IDs</name>
  <files>
    lib/features/markets/data/datasources/market_mock_datasource.dart,
    lib/features/markets/data/mock_markets.dart
  </files>
  <read_first>
    - C:/dev/fiyatcep/lib/features/markets/data/datasources/market_mock_datasource.dart
    - C:/dev/fiyatcep/lib/features/markets/data/mock_markets.dart
  </read_first>
  <action>
In both files, replace the five numeric IDs with brand-name slugs.

**ID mapping:**
- `id: 'm1'` → `id: 'migros'`  (name: 'Migros')
- `id: 'm2'` → `id: 'a101'`   (name: 'A101')
- `id: 'm3'` → `id: 'bim'`    (name: 'BİM')
- `id: 'm4'` → `id: 'sok'`    (name: 'ŞOK')
- `id: 'm5'` → `id: 'carrefoursa'` (name: 'CarrefourSA')

**File 1: `lib/features/markets/data/datasources/market_mock_datasource.dart`**

Change each `id:` field in the MarketItem entries. No other fields change. The `getMarketById` method uses `m.id == id` — this still works correctly because callers now pass slug IDs from the market object itself.

**File 2: `lib/features/markets/data/mock_markets.dart`**

Same five `id:` field replacements. No other fields change.

Do NOT change any `name`, `description`, `branchCount`, `activeDiscountCount`, `supportsOnlineOrder`, or `hasLoyaltyProgram` values.
  </action>
  <verify>
    <automated>cd C:/dev/fiyatcep && grep -n "id: 'migros'\|id: 'a101'\|id: 'bim'\|id: 'sok'\|id: 'carrefoursa'" lib/features/markets/data/datasources/market_mock_datasource.dart lib/features/markets/data/mock_markets.dart</automated>
  </verify>
  <done>Both market mock files use slug IDs. grep confirms all 5 slugs present in each file. No 'm1'–'m5' IDs remain in these two files.</done>
</task>

<task type="auto">
  <name>Task 2: Update product and discount mock files with slug marketIds</name>
  <files>
    lib/features/products/data/datasources/product_mock_datasource.dart,
    lib/features/products/data/mock_products.dart,
    lib/features/products/data/mock_market_prices.dart,
    lib/features/discounts/data/datasources/discount_mock_datasource.dart,
    lib/features/discounts/data/mock_discounts.dart
  </files>
  <read_first>
    - C:/dev/fiyatcep/lib/features/products/data/datasources/product_mock_datasource.dart
    - C:/dev/fiyatcep/lib/features/products/data/mock_products.dart
    - C:/dev/fiyatcep/lib/features/products/data/mock_market_prices.dart
    - C:/dev/fiyatcep/lib/features/discounts/data/datasources/discount_mock_datasource.dart
    - C:/dev/fiyatcep/lib/features/discounts/data/mock_discounts.dart
  </read_first>
  <action>
In all five files, replace `marketId: 'm1'` → `marketId: 'migros'` and so on for all five markets.

**ID mapping:**
- `marketId: 'm1'` → `marketId: 'migros'`
- `marketId: 'm2'` → `marketId: 'a101'`
- `marketId: 'm3'` → `marketId: 'bim'`
- `marketId: 'm4'` → `marketId: 'sok'`
- `marketId: 'm5'` → `marketId: 'carrefoursa'`

**File 1: `lib/features/products/data/datasources/product_mock_datasource.dart`**

Update `marketId:` fields in all ProductItem entries (p1–p6). The `getProductsByMarket` filter `p.marketId == marketId` continues to work correctly because callers pass market.id from the now-updated MarketItem.

**File 2: `lib/features/products/data/mock_products.dart`**

Same marketId replacements for p1–p6. Do NOT remove or add priceHistory data — this file may have already been updated by plan 03-04.

**File 3: `lib/features/products/data/mock_market_prices.dart`**

Replace all 16 `marketId:` occurrences (spread across p1–p6 entries). No other fields change.

**File 4: `lib/features/discounts/data/datasources/discount_mock_datasource.dart`**

Replace the 5 `marketId:` fields in DiscountItem entries (d1–d5). The `getDiscountsByMarket` filter continues to work correctly.

**File 5: `lib/features/discounts/data/mock_discounts.dart`**

Replace the 5 `marketId:` fields in DiscountItem entries (d1, d2, d3, d4, d5). No other fields change.

Do NOT change any `id:`, `productId:`, `name`, `market:`, `marketName:`, `price`, or other non-marketId fields.
  </action>
  <verify>
    <automated>cd C:/dev/fiyatcep && grep -rn "marketId: 'm[0-9]'" lib/features/products/data/ lib/features/discounts/data/</automated>
  </verify>
  <done>No `marketId: 'm1'`–`marketId: 'm5'` patterns remain in any product or discount data file. All marketId values now use slug strings.</done>
</task>

<task type="auto">
  <name>Task 3: Update tests to use slug IDs and run full test suite</name>
  <files>
    test/features/markets/data/repositories/market_repository_test.dart,
    test/features/discounts/data/repositories/discount_repository_test.dart,
    test/features/discounts/models/discount_item_test.dart,
    test/features/products/data/repositories/product_repository_test.dart,
    test/features/products/presentation/pages/product_detail_page_test.dart,
    test/features/favorites/presentation/providers/favorites_notifier_test.dart
  </files>
  <read_first>
    - C:/dev/fiyatcep/test/features/markets/data/repositories/market_repository_test.dart
    - C:/dev/fiyatcep/test/features/discounts/data/repositories/discount_repository_test.dart
    - C:/dev/fiyatcep/test/features/discounts/models/discount_item_test.dart
    - C:/dev/fiyatcep/test/features/products/data/repositories/product_repository_test.dart
    - C:/dev/fiyatcep/test/features/products/presentation/pages/product_detail_page_test.dart
    - C:/dev/fiyatcep/test/features/favorites/presentation/providers/favorites_notifier_test.dart
  </read_first>
  <action>
Update ID literals in test files to match the new slug IDs. Only change the ID strings — test logic, assertions, and structure must remain identical.

**Changes per file:**

`test/features/markets/data/repositories/market_repository_test.dart`:
- Line 70: `repo.getMarketById('m1')` → `repo.getMarketById('migros')`
- Line 74: `expect(data.id, equals('m1'))` → `expect(data.id, equals('migros'))`

`test/features/discounts/data/repositories/discount_repository_test.dart`:
- Line 70: `repo.getDiscountsByMarket('m1')` → `repo.getDiscountsByMarket('migros')`
- Line 75: `d.marketId == 'm1'` → `d.marketId == 'migros'`
- The "unknown marketId returns empty list" test (line 82) tests an ID that does NOT exist — any non-slug value works, e.g. keep `'unknown-market'` or change `'m99'` to `'unknown-market'`. Do not use a valid slug there.

`test/features/discounts/models/discount_item_test.dart`:
- `marketId: 'm1'` → `marketId: 'migros'`

`test/features/products/data/repositories/product_repository_test.dart`:
- Line 97: `repo.getProductsByMarket('m1')` → `repo.getProductsByMarket('migros')`
- Line 103: `product.marketId, equals('m1')` → `product.marketId, equals('migros')`

`test/features/products/presentation/pages/product_detail_page_test.dart`:
- `marketId: 'm2'` → `marketId: 'a101'`

`test/features/favorites/presentation/providers/favorites_notifier_test.dart`:
- All occurrences of `marketId: 'm1'` → `marketId: 'migros'`
- All occurrences of `marketId: 'm2'` → `marketId: 'a101'`
- The JSON string on line 44 `"marketId":"m1"` → `"marketId":"migros"`

After all edits, run the full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd C:/dev/fiyatcep && flutter test --no-pub 2>&1 | tail -15</automated>
  </verify>
  <done>All tests pass. No 'm1'–'m5' ID literals remain in any test file. `flutter test --no-pub` exits with 0 failures.</done>
</task>

</tasks>

<verification>
- `grep -rn "id: 'm[0-9]'" lib/features/markets/` returns no matches
- `grep -rn "marketId: 'm[0-9]'" lib/` returns no matches
- `grep -rn "'m[0-9]'" test/` returns no matches (all test ID literals updated)
- `flutter analyze lib/` passes with no errors
- `flutter test --no-pub` passes — all existing tests green
- At runtime: navigating to Migros market detail shows an orange-blue (#004A97) banner, not grey
- At runtime: logo placeholder shows Icons.store in brand blue (#004A97), not grey
</verification>

<success_criteria>
- Mock market IDs use brand slugs: migros, a101, bim, sok, carrefoursa
- All downstream mock data (products, market prices, discounts) use matching slug marketIds
- All test files updated to use the same slugs — no broken assertions
- `flutter test --no-pub` passes with zero failures
- `marketBrands[market.id]` lookup in MarketDetailHeaderWidget now returns the correct MarketBrand for all 5 markets
- UAT test 5: market detail page shows solid brand-colored banner (not grey)
- UAT test 6: logo placeholder icon appears in brand color (not grey)
</success_criteria>

<output>
After completion, create `.planning/phases/03-price-comparison-market-detail/03-05-SUMMARY.md`
</output>
