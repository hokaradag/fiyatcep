---
phase: 01-quality-foundation
plan: 05
type: gap_closure
gaps_addressed: [8]
wave: 1
depends_on: []
files_modified:
  - lib/core/utils/text_normalizer.dart
  - lib/features/discounts/discounts_page.dart
  - test/core/utils/text_normalizer_test.dart
autonomous: true
requirements: [QUAL-01]
gap_closure: true
must_haves:
  truths:
    - "Turkish uppercase characters (İ, Ç, Ğ, Ö, Ş, Ü) normalize correctly to ASCII equivalents"
    - "No duplicate _normalizeText() exists anywhere in the codebase"
    - "Discounts page search uses TextNormalizer.normalize() for filtering"
  artifacts:
    - path: "lib/core/utils/text_normalizer.dart"
      provides: "Turkish character normalization with pre-lowercase uppercase replacements"
      contains: "replaceAll('İ', 'i')"
    - path: "test/core/utils/text_normalizer_test.dart"
      provides: "Tests covering uppercase Turkish İ and other uppercase chars"
      contains: "İstanbul"
  key_links:
    - from: "lib/features/discounts/discounts_page.dart"
      to: "lib/core/utils/text_normalizer.dart"
      via: "import and TextNormalizer.normalize() calls"
      pattern: "TextNormalizer\\.normalize"
---

<objective>
Fix Turkish capital dotted-I (İ, U+0130) normalization defect and eliminate duplicate _normalizeText() in discounts_page.dart.

Purpose: UAT test 8 found that Turkish special characters cannot be reliably typed/matched in search. The root cause is that `İ`.toLowerCase() does not produce `i` on all Flutter target platforms, and discounts_page.dart has a private duplicate normalizer with the same defect.
Output: TextNormalizer handles all Turkish uppercase characters before toLowerCase(), discounts_page.dart delegates to TextNormalizer, all tests pass.
</objective>

<execution_context>
@C:/dev/fiyatcep/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/fiyatcep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-quality-foundation/01-01-SUMMARY.md

<interfaces>
From lib/core/utils/text_normalizer.dart:
```dart
class TextNormalizer {
  static String normalize(String text) {
    return text
        .toLowerCase()
        .replaceAll('ç', 'c')
        .replaceAll('ğ', 'g')
        .replaceAll('ı', 'i')
        .replaceAll('ö', 'o')
        .replaceAll('ş', 's')
        .replaceAll('ü', 'u');
  }
}
```

From lib/features/discounts/discounts_page.dart (lines 34-44):
```dart
  String _normalizeText(String text) {
    return text
        .toLowerCase()
        .replaceAll('ç', 'c')
        .replaceAll('ğ', 'g')
        .replaceAll('ı', 'i')
        .replaceAll('ö', 'o')
        .replaceAll('ş', 's')
        .replaceAll('ü', 'u')
        .trim();
  }
```
The private method is called at lines 54, 55, 56 for search filtering.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix TextNormalizer uppercase Turkish character handling</name>
  <files>lib/core/utils/text_normalizer.dart, test/core/utils/text_normalizer_test.dart</files>
  <behavior>
    - TextNormalizer.normalize('İstanbul') returns 'istanbul' (not a garbled multi-char sequence)
    - TextNormalizer.normalize('İ') returns 'i'
    - TextNormalizer.normalize('Ç') returns 'c'
    - TextNormalizer.normalize('Ğ') returns 'g'
    - TextNormalizer.normalize('Ö') returns 'o'
    - TextNormalizer.normalize('Ş') returns 's'
    - TextNormalizer.normalize('Ü') returns 'u'
    - TextNormalizer.normalize('ŞOK') returns 'sok'
    - All existing tests continue to pass
  </behavior>
  <action>
    1. Add new test cases to test/core/utils/text_normalizer_test.dart:
       - A test group 'handles uppercase Turkish characters' with assertions for İ, Ç, Ğ, Ö, Ş, Ü
       - Specific test: normalize('İstanbul') == 'istanbul'
       - Specific test: normalize('ŞOK') == 'sok' (real-world UAT scenario)
    2. Run tests — new tests MUST fail (RED phase).
    3. Fix lib/core/utils/text_normalizer.dart:
       - Add uppercase Turkish replacements BEFORE the .toLowerCase() call
       - The order must be: first replace uppercase special chars, then toLowerCase(), then replace lowercase special chars
       - Specific replacements to add before toLowerCase():
         .replaceAll('İ', 'i')
         .replaceAll('Ç', 'c')
         .replaceAll('Ğ', 'g')
         .replaceAll('Ö', 'o')
         .replaceAll('Ş', 's')
         .replaceAll('Ü', 'u')
       - Also add .trim() to match discounts_page behavior (will be needed after Task 2 eliminates the duplicate)
    4. Run tests — all tests MUST pass (GREEN phase).
  </action>
  <verify>
    <automated>cd C:/dev/fiyatcep && flutter test test/core/utils/text_normalizer_test.dart</automated>
  </verify>
  <done>TextNormalizer.normalize('İstanbul') returns 'istanbul', normalize('ŞOK') returns 'sok', all 14+ tests pass</done>
</task>

<task type="auto">
  <name>Task 2: Replace duplicate _normalizeText in discounts_page.dart with TextNormalizer</name>
  <files>lib/features/discounts/discounts_page.dart</files>
  <action>
    1. Add import at top of file: `import 'package:fiyatcep/core/utils/text_normalizer.dart';`
    2. Delete the private `_normalizeText()` method (lines 34-44).
    3. Replace all three call sites (lines 54, 55, 56) — change `_normalizeText(...)` to `TextNormalizer.normalize(...)`:
       - `final normalizedQuery = TextNormalizer.normalize(searchText);`
       - `final normalizedProductName = TextNormalizer.normalize(item.productName);`
       - `final normalizedMarketName = TextNormalizer.normalize(item.marketName);`
    4. Verify no other references to `_normalizeText` remain in the file.
    5. Run the full test suite to confirm no regressions.
  </action>
  <verify>
    <automated>cd C:/dev/fiyatcep && flutter test</automated>
  </verify>
  <done>No `_normalizeText` method exists in discounts_page.dart. All calls use TextNormalizer.normalize(). Full test suite passes (50+ tests, exit code 0). `grep -r "_normalizeText" lib/` returns zero results.</done>
</task>

</tasks>

<verification>
1. `flutter test` — all tests pass (exit code 0)
2. `grep -r "_normalizeText" lib/` — returns no matches (no duplicate normalizers remain)
3. `grep "replaceAll('İ'" lib/core/utils/text_normalizer.dart` — confirms İ handling exists
</verification>

<success_criteria>
- TextNormalizer correctly normalizes İ (U+0130) and all uppercase Turkish characters before toLowerCase()
- Zero duplicate _normalizeText() methods in codebase (only TextNormalizer.normalize exists)
- discounts_page.dart imports and uses TextNormalizer.normalize() for search filtering
- Full test suite passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/01-quality-foundation/01-05-SUMMARY.md`
</output>
