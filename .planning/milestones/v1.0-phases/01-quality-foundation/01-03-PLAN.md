---
phase: 01-quality-foundation
plan: 03
type: execute
wave: 2
depends_on: [01, 02]
files_modified:
  - lib/features/home/home_page.dart
  - lib/features/home/widgets/home_header_widget.dart
  - lib/features/home/widgets/home_stats_section.dart
  - lib/features/home/widgets/home_markets_section.dart
  - lib/features/home/widgets/home_discounts_section.dart
  - lib/features/markets/presentation/pages/market_detail_page.dart
  - lib/features/markets/widgets/market_detail_header_widget.dart
  - lib/features/markets/widgets/market_products_section.dart
  - lib/features/markets/widgets/market_discounts_section.dart
  - lib/features/products/presentation/pages/product_detail_page.dart
  - lib/features/products/widgets/product_info_section.dart
  - lib/features/products/widgets/product_price_section.dart
autonomous: true
requirements: [QUAL-03]

must_haves:
  truths:
    - "home_page.dart build() method delegates to extracted widget classes, not inline widget trees"
    - "market_detail_page.dart build() method delegates to extracted widget classes"
    - "product_detail_page.dart build() method delegates to extracted widget classes"
    - "Each extracted widget is a StatelessWidget or ConsumerWidget in its own file"
  artifacts:
    - path: "lib/features/home/widgets/"
      provides: "Extracted home page section widgets"
    - path: "lib/features/markets/widgets/"
      provides: "Extracted market detail section widgets"
    - path: "lib/features/products/widgets/"
      provides: "Extracted product detail section widgets"
  key_links:
    - from: "lib/features/home/home_page.dart"
      to: "lib/features/home/widgets/"
      via: "imports and widget instantiation in build()"
      pattern: "Home.*Widget|Home.*Section"
    - from: "lib/features/markets/presentation/pages/market_detail_page.dart"
      to: "lib/features/markets/widgets/"
      via: "imports and widget instantiation in build()"
      pattern: "MarketDetail.*Widget|Market.*Section"
    - from: "lib/features/products/presentation/pages/product_detail_page.dart"
      to: "lib/features/products/widgets/"
      via: "imports and widget instantiation in build()"
      pattern: "Product.*Section"
---

<objective>
Decompose large page build() methods into extracted StatelessWidget classes in separate files.

Purpose: Break down the 3 largest pages (home 432 lines, market_detail 416 lines, product_detail 270 lines) into manageable, independently testable widget components (QUAL-03). This makes the codebase readable and maintainable for feature development in subsequent phases.

Output: 8-10 new widget files in `widgets/` directories, 3 simplified page files with delegating build() methods.
</objective>

<execution_context>
@C:/dev/fiyatcep/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/fiyatcep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-quality-foundation/01-CONTEXT.md
@.planning/phases/01-quality-foundation/01-RESEARCH.md
@.planning/phases/01-quality-foundation/01-01-SUMMARY.md
@.planning/phases/01-quality-foundation/01-02-SUMMARY.md

<interfaces>
<!-- Existing pattern to follow for extracted widgets -->

From lib/features/markets/widgets/market_card.dart (existing extracted widget pattern):
```dart
class MarketCard extends StatelessWidget {
  final MarketItem market;
  const MarketCard({super.key, required this.market});

  @override
  Widget build(BuildContext context) { ... }
}
```

Widget naming convention:
- *_widget.dart for standalone components
- *_section.dart for page sections
- *_card.dart for list item widgets
- All use PascalCase class names, snake_case file names
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decompose HomePage into extracted widgets (QUAL-03)</name>
  <files>
    lib/features/home/home_page.dart,
    lib/features/home/widgets/home_header_widget.dart,
    lib/features/home/widgets/home_stats_section.dart,
    lib/features/home/widgets/home_markets_section.dart,
    lib/features/home/widgets/home_discounts_section.dart
  </files>
  <read_first>
    lib/features/home/home_page.dart,
    lib/features/markets/widgets/market_card.dart
  </read_first>
  <action>
    **Per QUAL-03 and Claude's Discretion on file layout: place in `widgets/` subdirectory matching existing `markets/widgets/` pattern.**

    Read `home_page.dart` (432 lines) and identify the major sections of its `build()` method. Create the `lib/features/home/widgets/` directory. Extract each major section into a separate `StatelessWidget` (or `ConsumerWidget` if it uses `ref`).

    **Extraction targets (adjust based on actual code structure found in the file):**

    1. **`HomeHeaderWidget`** (`home_header_widget.dart`): The app bar or header section at the top of the home page. Receives any needed data via constructor parameters.

    2. **`HomeStatsSection`** (`home_stats_section.dart`): The statistics/summary cards section (product count, market count, favorites count, discount count). This section uses favorites data — after Plan 02, it will use `ref.watch(favoritesNotifierProvider)`. If it needs `ref`, make it a `ConsumerWidget`.

    3. **`HomeMarketsSection`** (`home_markets_section.dart`): The markets carousel/list section. Receives `List<MarketItem>` or `AsyncValue` via constructor.

    4. **`HomeDiscountsSection`** (`home_discounts_section.dart`): The discounts preview section. Receives discount data via constructor.

    **Rules:**
    - Each extracted widget is a `StatelessWidget` unless it needs `ref` (then `ConsumerWidget`)
    - Pass data via constructor parameters — do not duplicate provider watches
    - Do NOT extract private helper methods (`Widget _buildX()`) — create proper widget classes
    - After extraction, `home_page.dart` build() should be short: assembling the extracted widgets in a column/scaffold
    - Keep any page-level state management (search text, tab state) in the page itself
    - Each new file gets proper imports for its dependencies (models, providers if needed)

    **After extraction, `home_page.dart` build() should look approximately like:**
    ```dart
    @override
    Widget build(BuildContext context) {
      return Scaffold(
        body: SingleChildScrollView(
          child: Column(
            children: [
              const HomeHeaderWidget(),
              HomeStatsSection(...),
              HomeMarketsSection(...),
              HomeDiscountsSection(...),
            ],
          ),
        ),
      );
    }
    ```
  </action>
  <verify>
    <automated>flutter analyze lib/features/home/</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/home/widgets/ directory exists
    - lib/features/home/widgets/home_header_widget.dart exists and contains `class HomeHeaderWidget extends`
    - lib/features/home/widgets/home_stats_section.dart exists and contains `class HomeStatsSection extends`
    - lib/features/home/widgets/home_markets_section.dart exists and contains `class HomeMarketsSection extends`
    - lib/features/home/widgets/home_discounts_section.dart exists and contains `class HomeDiscountsSection extends`
    - home_page.dart build() method is under 80 lines (down from 432 total file)
    - home_page.dart imports from `widgets/` directory
    - grep -r 'Widget _build' lib/features/home/home_page.dart returns no results (no private build helpers remain)
    - flutter analyze lib/features/home/ reports no errors
  </acceptance_criteria>
  <done>HomePage is decomposed into 4 extracted widget classes in separate files. build() method is a short assembly of child widgets.</done>
</task>

<task type="auto">
  <name>Task 2: Decompose MarketDetailPage and ProductDetailPage into extracted widgets (QUAL-03)</name>
  <files>
    lib/features/markets/presentation/pages/market_detail_page.dart,
    lib/features/markets/widgets/market_detail_header_widget.dart,
    lib/features/markets/widgets/market_products_section.dart,
    lib/features/markets/widgets/market_discounts_section.dart,
    lib/features/products/presentation/pages/product_detail_page.dart,
    lib/features/products/widgets/product_info_section.dart,
    lib/features/products/widgets/product_price_section.dart
  </files>
  <read_first>
    lib/features/markets/presentation/pages/market_detail_page.dart,
    lib/features/products/presentation/pages/product_detail_page.dart,
    lib/features/markets/widgets/market_card.dart
  </read_first>
  <action>
    **MarketDetailPage decomposition (416 lines):**

    Read `market_detail_page.dart` and identify major sections. Extract into `lib/features/markets/widgets/` (directory already exists):

    1. **`MarketDetailHeaderWidget`** (`market_detail_header_widget.dart`): Market logo, name, banner section at top. Receives `MarketItem` via constructor.

    2. **`MarketProductsSection`** (`market_products_section.dart`): The product list for this market. Receives product data via constructor or watches provider if needed.

    3. **`MarketDiscountsSection`** (`market_discounts_section.dart`): Discounts list filtered to this market. Receives discount data via constructor.

    After extraction, `market_detail_page.dart` build() should be a short scaffold assembling these widgets.

    **ProductDetailPage decomposition (270 lines):**

    Read `product_detail_page.dart` and identify major sections. Create `lib/features/products/widgets/` directory. Extract:

    1. **`ProductInfoSection`** (`product_info_section.dart`): Product name, brand, image placeholder, basic info. Receives `ProductItem` via constructor.

    2. **`ProductPriceSection`** (`product_price_section.dart`): Market price comparison rows/cards. Receives price list via constructor.

    After extraction, `product_detail_page.dart` build() should be short, assembling the extracted widgets.

    **Same rules as Task 1:**
    - Each widget is `StatelessWidget` unless it needs `ref` (then `ConsumerWidget`)
    - Pass data via constructor, do not duplicate provider watches
    - Create proper widget classes, NOT private helper methods
    - Keep page-level state (favorites toggle, navigation) in the page
    - Adapt the exact extraction points based on what you find in the actual code — the section names above are guidance, not prescriptive
  </action>
  <verify>
    <automated>flutter analyze lib/features/markets/ lib/features/products/</automated>
  </verify>
  <acceptance_criteria>
    - lib/features/markets/widgets/market_detail_header_widget.dart exists and contains `class MarketDetailHeaderWidget extends`
    - lib/features/markets/widgets/market_products_section.dart exists and contains `class MarketProductsSection extends`
    - lib/features/markets/widgets/market_discounts_section.dart exists and contains `class MarketDiscountsSection extends`
    - lib/features/products/widgets/ directory exists
    - lib/features/products/widgets/product_info_section.dart exists and contains `class ProductInfoSection extends`
    - lib/features/products/widgets/product_price_section.dart exists and contains `class ProductPriceSection extends`
    - market_detail_page.dart build() method is significantly shorter (page total under ~150 lines, down from 416)
    - product_detail_page.dart build() method is significantly shorter (page total under ~120 lines, down from 270)
    - flutter analyze lib/features/markets/ reports no errors
    - flutter analyze lib/features/products/ reports no errors
  </acceptance_criteria>
  <done>MarketDetailPage decomposed into 3 extracted widgets, ProductDetailPage decomposed into 2 extracted widgets. All pages have short, readable build() methods.</done>
</task>

</tasks>

<verification>
- `flutter analyze` passes with no errors across all feature directories
- All 3 pages (home, market_detail, product_detail) have build() methods that primarily compose extracted widgets
- No private `Widget _build*()` helper methods remain in the 3 page files
- All extracted widgets are proper classes extending StatelessWidget or ConsumerWidget
- Extracted widget files are in `widgets/` subdirectories following existing convention
</verification>

<success_criteria>
- home_page.dart, market_detail_page.dart, product_detail_page.dart are all significantly shorter
- 8-10 new widget files exist in widgets/ directories
- Each extracted widget is independently readable and testable
- flutter analyze clean across all modified files
- App renders identically (no visual changes — pure refactoring)
</success_criteria>

<output>
After completion, create `.planning/phases/01-quality-foundation/01-03-SUMMARY.md`
</output>
