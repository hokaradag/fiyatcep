---
phase: 01-quality-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/core/utils/text_normalizer.dart
  - lib/features/products/data/datasources/product_mock_datasource.dart
  - lib/features/markets/data/datasources/market_mock_datasource.dart
  - lib/features/discounts/data/datasources/discount_mock_datasource.dart
  - lib/features/markets/widgets/market_card.dart
  - lib/features/products/presentation/providers/products_provider.dart
  - lib/features/markets/presentation/providers/markets_provider.dart
  - lib/features/discounts/presentation/providers/discounts_provider.dart
autonomous: true
requirements: [QUAL-01, QUAL-02, QUAL-07]

must_haves:
  truths:
    - "All Turkish character normalization uses a single TextNormalizer.normalize() call — no private _normalizeText copies remain"
    - "Providers throw typed AppException instead of raw Exception — error messages are meaningful"
    - "CarrefourSA spelling is consistent across all mock datasources — no 'Carrefoursa' variants exist"
  artifacts:
    - path: "lib/core/utils/text_normalizer.dart"
      provides: "Centralized Turkish text normalization utility"
      contains: "class TextNormalizer"
    - path: "lib/features/products/presentation/providers/products_provider.dart"
      provides: "Typed error propagation in product providers"
      contains: "AppException"
    - path: "lib/features/markets/presentation/providers/markets_provider.dart"
      provides: "Typed error propagation in market providers"
      contains: "AppException"
    - path: "lib/features/discounts/presentation/providers/discounts_provider.dart"
      provides: "Typed error propagation in discount providers"
      contains: "AppException"
  key_links:
    - from: "lib/features/products/data/datasources/product_mock_datasource.dart"
      to: "lib/core/utils/text_normalizer.dart"
      via: "import and TextNormalizer.normalize() call"
      pattern: "TextNormalizer\\.normalize"
    - from: "lib/features/markets/data/datasources/market_mock_datasource.dart"
      to: "lib/core/utils/text_normalizer.dart"
      via: "import and TextNormalizer.normalize() call"
      pattern: "TextNormalizer\\.normalize"
    - from: "lib/features/discounts/data/datasources/discount_mock_datasource.dart"
      to: "lib/core/utils/text_normalizer.dart"
      via: "import and TextNormalizer.normalize() call"
      pattern: "TextNormalizer\\.normalize"
    - from: "lib/features/markets/widgets/market_card.dart"
      to: "lib/core/utils/text_normalizer.dart"
      via: "import and TextNormalizer.normalize() call"
      pattern: "TextNormalizer\\.normalize"
---

<objective>
Extract TextNormalizer utility, normalize CarrefourSA naming, and fix typed error propagation in providers.

Purpose: Eliminate duplicate utility code (QUAL-01), fix inconsistent brand naming (QUAL-07), and ensure users see meaningful error messages instead of opaque Exception strings (QUAL-02). These are foundational code quality fixes that unblock safe feature development.

Output: `lib/core/utils/text_normalizer.dart` utility class, 3 provider files with typed AppException throws, 3 datasource files with consistent CarrefourSA naming.
</objective>

<execution_context>
@C:/dev/fiyatcep/.claude/get-shit-done/workflows/execute-plan.md
@C:/dev/fiyatcep/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-quality-foundation/01-CONTEXT.md
@.planning/phases/01-quality-foundation/01-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs -->

From lib/core/errors/exceptions.dart:
```dart
class AppException implements Exception {
  final String message;
  final String? code;
  final dynamic originalException;

  AppException({required this.message, this.code, this.originalException});
}
// Subclasses: NetworkException, ServerException, ClientException, ParseException, CacheException
```

From lib/core/errors/result.dart:
```dart
// Sealed Result<T> with .when() method:
// success: (data) => ..., failure: (message, code) => ..., loading: () => ...
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract TextNormalizer utility and normalize CarrefourSA naming (QUAL-01, QUAL-07)</name>
  <files>
    lib/core/utils/text_normalizer.dart,
    lib/features/products/data/datasources/product_mock_datasource.dart,
    lib/features/markets/data/datasources/market_mock_datasource.dart,
    lib/features/discounts/data/datasources/discount_mock_datasource.dart,
    lib/features/markets/widgets/market_card.dart
  </files>
  <read_first>
    lib/features/products/data/datasources/product_mock_datasource.dart,
    lib/features/markets/data/datasources/market_mock_datasource.dart,
    lib/features/discounts/data/datasources/discount_mock_datasource.dart,
    lib/features/markets/widgets/market_card.dart
  </read_first>
  <action>
    **Per D-01/D-02: no new dependencies.**

    1. Create `lib/core/utils/text_normalizer.dart` with this exact class (per QUAL-01):
    ```dart
    class TextNormalizer {
      static String normalize(String text) {
        return text
            .toLowerCase()
            .replaceAll('c', 'c')
            .replaceAll('g', 'g')
            .replaceAll('i', 'i')
            .replaceAll('o', 'o')
            .replaceAll('s', 's')
            .replaceAll('u', 'u');
      }
    }
    ```
    (Use the actual Turkish characters: c-cedilla, g-breve, dotless-i, o-umlaut, s-cedilla, u-umlaut as the first argument to each replaceAll. The research file has the exact character map.)

    2. In each of the 3 mock datasource files:
       - Add `import 'package:fiyatcep/core/utils/text_normalizer.dart';`
       - Replace all calls to `_normalizeText(...)` with `TextNormalizer.normalize(...)`
       - Delete the private `_normalizeText()` method definition
       - QUAL-07: Change any `'Carrefoursa'` string literal to `'CarrefourSA'`. Specifically:
         - `product_mock_datasource.dart` line ~52: `'Carrefoursa'` -> `'CarrefourSA'`
         - `market_mock_datasource.dart` line ~48: `'Carrefoursa'` -> `'CarrefourSA'`
         - `discount_mock_datasource.dart` line ~48: `'Carrefoursa'` -> `'CarrefourSA'`

    3. In `market_card.dart`:
       - Add `import 'package:fiyatcep/core/utils/text_normalizer.dart';`
       - Replace calls to `_normalizeName(...)` with `TextNormalizer.normalize(...)`
       - Delete the private `_normalizeName()` method definition
       - Do NOT change the switch-case string `'carrefoursa'` — it is the normalized form and remains correct after TextNormalizer.normalize('CarrefourSA') produces 'carrefoursa'
  </action>
  <verify>
    <automated>flutter analyze lib/core/utils/text_normalizer.dart lib/features/products/data/datasources/product_mock_datasource.dart lib/features/markets/data/datasources/market_mock_datasource.dart lib/features/discounts/data/datasources/discount_mock_datasource.dart lib/features/markets/widgets/market_card.dart</automated>
  </verify>
  <acceptance_criteria>
    - lib/core/utils/text_normalizer.dart exists and contains `class TextNormalizer`
    - lib/core/utils/text_normalizer.dart contains `static String normalize(String text)`
    - product_mock_datasource.dart contains `TextNormalizer.normalize` (not `_normalizeText`)
    - market_mock_datasource.dart contains `TextNormalizer.normalize` (not `_normalizeText`)
    - discount_mock_datasource.dart contains `TextNormalizer.normalize` (not `_normalizeText`)
    - market_card.dart contains `TextNormalizer.normalize` (not `_normalizeName`)
    - grep -r '_normalizeText' lib/ returns no results
    - grep -r '_normalizeName' lib/ returns no results
    - grep -r 'Carrefoursa' lib/ returns no results (only 'CarrefourSA' or 'carrefoursa' in switch-cases)
    - flutter analyze reports no errors on modified files
  </acceptance_criteria>
  <done>Single TextNormalizer utility used everywhere, no duplicate private normalize methods, all mock datasources use canonical 'CarrefourSA' spelling</done>
</task>

<task type="auto">
  <name>Task 2: Fix typed error propagation in all providers (QUAL-02)</name>
  <files>
    lib/features/products/presentation/providers/products_provider.dart,
    lib/features/markets/presentation/providers/markets_provider.dart,
    lib/features/discounts/presentation/providers/discounts_provider.dart
  </files>
  <read_first>
    lib/features/products/presentation/providers/products_provider.dart,
    lib/features/markets/presentation/providers/markets_provider.dart,
    lib/features/discounts/presentation/providers/discounts_provider.dart,
    lib/core/errors/exceptions.dart,
    lib/core/errors/result.dart
  </read_first>
  <action>
    **Per QUAL-02: users must see meaningful error messages, not opaque Exception strings.**

    In all 3 provider files, make these changes to every `.when()` call on `Result<T>`:

    1. **Failure branch** — replace:
       ```dart
       failure: (message, code) => throw Exception(message),
       ```
       with:
       ```dart
       failure: (message, code) => throw AppException(message: message, code: code),
       ```
       Add `import 'package:fiyatcep/core/errors/exceptions.dart';` at the top of each file.

    2. **Loading branch** — replace:
       ```dart
       loading: () => throw Exception('Loading'),
       ```
       with:
       ```dart
       loading: () => throw StateError('Unexpected loading state in provider'),
       ```
       This is dead code (repositories never return LoadingResult) but must be safe.

    **Exact occurrences to fix:**

    `products_provider.dart` — 6 occurrences (3 failure branches, 3 loading branches):
    - `productsProvider`: failure + loading
    - `productSearchProvider`: failure + loading
    - `productByIdProvider`: failure + loading

    `markets_provider.dart` — 3 failure branches (check for loading branches too):
    - `marketsProvider`: failure
    - `marketSearchProvider`: failure
    - `marketByIdProvider`: failure

    `discounts_provider.dart` — 3 failure branches (check for loading branches too):
    - `discountsProvider`: failure
    - `discountSearchProvider`: failure
    - `discountsByMarketProvider`: failure

    After these changes, when a network error occurs, the UI's `.when(error: (e, st) => ...)` receives an `AppException` whose `.message` is a Turkish-language error string (e.g., "Internet baglantisi bulunamadi") instead of a generic `Exception` wrapper.
  </action>
  <verify>
    <automated>flutter analyze lib/features/products/presentation/providers/products_provider.dart lib/features/markets/presentation/providers/markets_provider.dart lib/features/discounts/presentation/providers/discounts_provider.dart</automated>
  </verify>
  <acceptance_criteria>
    - grep -r "throw Exception(" lib/features/products/presentation/providers/ returns no results
    - grep -r "throw Exception(" lib/features/markets/presentation/providers/ returns no results
    - grep -r "throw Exception(" lib/features/discounts/presentation/providers/ returns no results
    - products_provider.dart contains `throw AppException(message: message, code: code)` (at least 3 occurrences)
    - products_provider.dart contains `import 'package:fiyatcep/core/errors/exceptions.dart'`
    - markets_provider.dart contains `throw AppException(message: message, code: code)`
    - discounts_provider.dart contains `throw AppException(message: message, code: code)`
    - All loading branches use `throw StateError` instead of `throw Exception('Loading')`
    - flutter analyze reports no errors on modified files
  </acceptance_criteria>
  <done>All providers throw typed AppException on failure; no raw Exception(message) throws remain in any provider file; UI will display meaningful error messages via AppException.message</done>
</task>

</tasks>

<verification>
- `flutter analyze` passes with no errors
- `grep -r '_normalizeText\|_normalizeName' lib/` returns empty (no duplicate utility methods)
- `grep -r 'Carrefoursa' lib/` returns empty (only 'CarrefourSA' canonical form)
- `grep -r 'throw Exception(' lib/features/*/presentation/providers/` returns empty (no raw Exception throws)
- `lib/core/utils/text_normalizer.dart` exists with `TextNormalizer` class
</verification>

<success_criteria>
- TextNormalizer.normalize() is the single source of Turkish text normalization across all datasources and widgets
- All 3 mock datasources use 'CarrefourSA' (not 'Carrefoursa')
- All provider failure branches throw AppException with message and code
- flutter analyze passes clean on all modified files
</success_criteria>

<output>
After completion, create `.planning/phases/01-quality-foundation/01-01-SUMMARY.md`
</output>
