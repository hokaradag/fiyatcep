---
phase: 04.2-backend-data-pipeline
plan: 01
subsystem: database
tags: [python, fastapi, sqlalchemy, sqlite, apscheduler, pytest, turkish-normalization]

# Dependency graph
requires:
  - phase: 04.1-live-data-foundation
    provides: DB-SCHEMA.sql (frozen schema with 5 tables + 7 market seed rows)
provides:
  - Python backend project installable from backend/pyproject.toml
  - SQLAlchemy ORM models for all 5 DB tables matching DB-SCHEMA.sql exactly
  - init_db() function executing frozen DB-SCHEMA.sql via sqlite3.executescript()
  - normalize_name() handling all 12 Turkish diacritical characters
  - upsert_product() for canonical cross-market product matching and MarketProduct linking
  - Abstract BaseScraper class defining scrape log contract
  - pytest fixtures (tmp_db, db_session) for isolated test DB sessions
  - 20 passing unit tests (normalizer + product matching)
affects:
  - 04.2-02 (Migros scraper uses BaseScraper, upsert_product, normalize_name)
  - 04.2-03 (scheduler uses init_db, BaseScraper contract)
  - 04.3 (FastAPI app, ORM models, DB session all wired for REST endpoints)

# Tech tracking
tech-stack:
  added:
    - fastapi (latest) — REST framework
    - uvicorn (latest) — ASGI server
    - sqlalchemy 2.0.48 — ORM for SQLite reads/writes
    - apscheduler 3.11.2 — in-process job scheduler
    - requests 2.33.1 — HTTP client for scraping (pre-installed)
    - beautifulsoup4 4.14.3 — HTML parsing fallback
    - pytest 9.0.2 — test framework
    - httpx — async test client
  patterns:
    - TDD Red-Green for both tasks
    - sqlite3.executescript() for multi-statement SQL schema init (not ORM create_all)
    - str.maketrans() for Turkish diacritic normalization (uppercase before lowercase)
    - Session flush without commit — caller batches commits
    - tmp_path pytest fixture for isolated temp DB in each test

key-files:
  created:
    - backend/pyproject.toml
    - backend/app/__init__.py
    - backend/app/database.py
    - backend/app/main.py
    - backend/app/models.py
    - backend/app/routers/__init__.py
    - backend/scraper/__init__.py
    - backend/scraper/base.py
    - backend/data/.gitkeep
    - backend/seeds/markets.sql
    - backend/tests/__init__.py
    - backend/tests/conftest.py
    - backend/tests/test_setup.py
    - backend/tests/test_normalizer.py
    - backend/tests/test_product_matching.py
  modified:
    - .gitignore (added Python/backend entries)

key-decisions:
  - "pyproject.toml uses unpinned/range versions (>=2.0, >=3.10,<4) to survive Python 3.14 resolution"
  - "setuptools package discovery configured explicitly (find include=['app*','scraper*']) to avoid flat-layout multi-package error"
  - "init_db() uses sqlite3.executescript() not SQLAlchemy create_all() — DB-SCHEMA.sql is frozen source of truth"
  - "normalize_name(): uppercase Turkish replacements via str.maketrans BEFORE .lower() — platform-consistent İ->I->i"
  - "upsert_product() flushes session, never commits — caller must commit the batch"
  - "upsert_product() returns market_product.id (Flutter ProductItem.id, D-02), never products.id"
  - "utc_now_str() uses datetime.now(timezone.utc) — never deprecated datetime.utcnow()"
  - ".gitignore updated with backend/*.egg-info/ (editable install artifact, not in plan but required)"

patterns-established:
  - "Pattern: normalize_name() — str.maketrans with 12 Turkish chars, uppercase before lowercase, then re.sub whitespace"
  - "Pattern: upsert_product() — filter_by(normalized_name, brand) lookup, flush without commit, return market_product.id"
  - "Pattern: conftest.py tmp_db — sqlite3.executescript(schema) in tmp_path, yields path; db_session builds SQLAlchemy engine on top"

requirements-completed: [PIPE-01, PIPE-02, PIPE-03]

# Metrics
duration: 18min
completed: 2026-04-02
---

# Phase 04.2 Plan 01: Python Backend Bootstrap Summary

**Python backend with FastAPI+SQLAlchemy+SQLite, 5 ORM models matching frozen DB-SCHEMA.sql, Turkish text normalizer handling all 12 diacritics, and canonical product matching — 20 unit tests passing**

## Performance

- **Duration:** ~18 min
- **Started:** 2026-04-02T20:50:00Z
- **Completed:** 2026-04-02T21:08:00Z
- **Tasks:** 2
- **Files modified:** 16

## Accomplishments

- Python backend project installable via `pip install -e ".[dev]"` from `backend/` using pyproject.toml with unpinned/range versions compatible with Python 3.14
- All 5 SQLAlchemy ORM models (Product, Market, MarketProduct, PriceHistory, Discount) match DB-SCHEMA.sql column names, types, constraints, and ForeignKey references exactly; MarketProduct has UniqueConstraint("product_id", "market_id")
- `init_db()` reads and executes the frozen DB-SCHEMA.sql via `sqlite3.executescript()` — guarantees exact schema + all 7 market seed rows without duplicating schema in ORM
- `normalize_name()` handles all 12 Turkish diacritical characters (6 lower: ğşıöüç, 6 upper: ĞŞİÖÜÇ) using `str.maketrans()` with uppercase replacements before `.lower()` for platform-consistent behavior
- `upsert_product()` implements canonical cross-market product matching: lookup by (normalized_name, brand), create/reuse Product, create/update MarketProduct, flush without commit, return market_product.id (Flutter's ProductItem.id per D-02)
- 20 unit tests passing: 10 normalizer tests + 6 product matching tests + 4 setup/infrastructure tests

## Task Commits

Each task was committed atomically:

1. **Task 1: Create Python project structure, install dependencies, DB engine + ORM models, schema init** - `df0ccef` (feat)
2. **Task 2: Turkish text normalizer + canonical product matching logic + unit tests** - `c336c43` (feat)

## Files Created/Modified

- `backend/pyproject.toml` — project metadata, dependencies (fastapi, sqlalchemy>=2.0, apscheduler>=3.10<4, etc.), pytest config with pythonpath=["."]
- `backend/app/database.py` — SQLite engine with check_same_thread=False, SessionLocal, DeclarativeBase, get_db() generator, init_db() via sqlite3.executescript()
- `backend/app/models.py` — 5 ORM models matching DB-SCHEMA.sql exactly
- `backend/app/main.py` — minimal FastAPI app with init_db on startup
- `backend/scraper/base.py` — normalize_name(), utc_now_str(), upsert_product(), abstract BaseScraper
- `backend/tests/conftest.py` — tmp_db fixture (sqlite3 + executescript), db_session fixture (SQLAlchemy on tmp_db)
- `backend/tests/test_normalizer.py` — 10 tests for all Turkish chars, whitespace, real product names
- `backend/tests/test_product_matching.py` — 6 tests for canonical matching semantics
- `backend/tests/test_setup.py` — 4 infrastructure tests (imports, tables, seed, index)
- `backend/seeds/markets.sql` — reference copy of 7-market INSERT statements from DB-SCHEMA.sql
- `.gitignore` — added Python backend entries (venv, pyc, db, jsonl, egg-info)

## Decisions Made

- **pyproject.toml unpinned versions:** Pinned versions from plan (fastapi==0.115.3, etc.) resolved to newer versions (fastapi 0.135.3, sqlalchemy 2.0.48, apscheduler 3.11.2) during pip install on Python 3.14. Used range specifiers instead of exact pins to let pip resolve compatible versions — all constraints met.
- **setuptools find directive:** Explicit `[tool.setuptools.packages.find] include = ["app*", "scraper*"]` required because setuptools refused multi-package flat-layout auto-discovery. Not in plan — added as Rule 3 (blocking) fix.
- **backend/*.egg-info/ gitignore entry:** Editable install creates `fiyatcep_backend.egg-info/` in `backend/` which was untracked. Added to .gitignore — not in plan but required for clean git status.
- **DeclarativeBase class (SQLAlchemy 2.0):** Used `class Base(DeclarativeBase): pass` (SQLAlchemy 2.0 style) instead of legacy `declarative_base()` call.

## Deviations from Plan

### Auto-fixed Issues

**1. [Rule 1 - Bug] Fixed setuptools flat-layout multi-package discovery error**
- **Found during:** Task 1 (install step)
- **Issue:** `pip install -e ".[dev]"` failed with "Multiple top-level packages discovered in a flat-layout" — setuptools refused to build with app/, scraper/, data/, seeds/ all at root level
- **Fix:** Added `[tool.setuptools.packages.find] include = ["app*", "scraper*"]` to pyproject.toml to explicitly scope which directories are Python packages
- **Files modified:** backend/pyproject.toml
- **Verification:** `pip install -e ".[dev]"` exits 0, all packages installed
- **Committed in:** df0ccef (Task 1 commit)

**2. [Rule 2 - Missing Critical] Added backend/*.egg-info/ to .gitignore**
- **Found during:** Task 1 (post-install git status check)
- **Issue:** Editable install generated `backend/fiyatcep_backend.egg-info/` directory which was untracked — would pollute commits if left unaddressed
- **Fix:** Added `backend/*.egg-info/` to .gitignore
- **Files modified:** .gitignore
- **Verification:** `git status` no longer shows egg-info as untracked
- **Committed in:** df0ccef (Task 1 commit)

---

**Total deviations:** 2 auto-fixed (1 bug, 1 missing critical gitignore entry)
**Impact on plan:** Both fixes required for correct build and clean repo state. No scope creep.

## Issues Encountered

- Python 3.14 was invoked as `python` not `python3` on this system — plan verification commands adjusted accordingly. Tests run correctly.
- Pinned versions in plan were reference targets; actual pip resolved to newer compatible versions (all within the same major versions as specified in locked decisions).

## User Setup Required

None — no external service configuration required. Python 3.14 and pip are pre-installed; `pip install -e ".[dev]"` from `backend/` is the only setup step.

## Next Phase Readiness

- Plan 04.2-02 (Migros scraper) can import `BaseScraper`, `upsert_product`, `normalize_name` from `scraper.base`
- Plan 04.2-03 (APScheduler) can import `init_db`, `get_db`, `SessionLocal` from `app.database`
- `conftest.py` fixtures are ready for all future scraper tests (isolated temp DB per test)
- No blockers

---
*Phase: 04.2-backend-data-pipeline*
*Completed: 2026-04-02*
